# 때마침 저장소·브라우저 QA 보고서

- 수행일: 2026-08-23 (Asia/Seoul)
- 회귀 결함 해결 및 재검증: 2026-08-24 (Asia/Seoul)
- 기준 브랜치: `codex/course-map-ui-polish`
- 브라우저: Codex In-app Browser의 Playwright API
- 우선 viewport: 390×844, 보조 viewport: 320×568
- 정상 연동 환경: FE `http://127.0.0.1:5174`, BE `http://localhost:8081`
- 2026-08-24 추가 회귀 환경: FE `http://localhost:5173`, BE `http://localhost:8080`
- 참고: 기존 FE 5173의 기본 프록시 대상 8080은 이 저장소가 아니라 `/Users/yujin/Project/WhatJang/server`가 점유하고 있었다. 5173에서 관찰된 공개 API 401은 제품 판정에서 제외하고, 이 저장소 전용 FE/BE를 별도 포트에 실행해 재검증했다.

## 1. 현재 서비스 구현 상태

| 기능 | 기획 의도 | 현재 구현 상태 | 관련 화면/API | 비고 |
|---|---|---|---|---|
| 이메일 회원가입·로그인 | 계정 생성, 입력 검증, 로그인 | 일부 구현 | `#/signup`, `#/login`, `/api/v1/auth/*` | FE/BE 연결은 존재한다. 기획의 비밀번호 확인 필드는 없고 성공 테스트는 테스트 계정 생성/자격 증명 부재로 차단됐다. |
| 자동 로그인 | 토큰 확인 후 자동 진입, 만료 복구 | 일부 구현 | localStorage JWT, `/api/v1/members/me` | JWT 저장·401 정리는 있으나 refresh-token 재발급은 없다. |
| 온보딩·취향 | 서비스 소개와 관심사 저장 | 일부 구현 | auth route group | 화면은 있으나 관심 카테고리 서버 저장·추천 반영은 확인되지 않는다. |
| 지도 탐색 | 지도 영역 장소, 필터, 혼잡도, 현재 위치 | 구현 완료 | `#/map`, `/api/places/map`, `/api/v1/crowding/*`, `/api/events` | 실제 지도·검색·혼잡 배지·선택 시트가 동작한다. 위치/길찾기 실기 권한은 미검증이다. |
| 통합 검색 | 내부 장소와 외부 지도 장소 통합 검색 | 구현 완료 | 지도 inline search, Explore search, `/api/places`, `/api/place-search/kakao` | 내부·카카오 결과를 통합 관련도 순으로 표시하며 `경복궁` 정확 일치 최상단을 실브라우저에서 확인했다. |
| Explore 큐레이션 | 트렌드·촬영 작품·행사 탐색 | 구현 완료 | `#/explore`, `/api/places/trends`, `/api/media-contents/filming-works`, `/api/events` | 장소·메뉴/근거·작품·행사명/장소 실시간 검색과 각 상세 이동을 확인했다. |
| 장소 상세 기본 정보 | 기본 정보·운영 정보·지도·트렌드 | 일부 구현 | `#/place/:id`, `/api/places/:id`, filming-location API | 기본 정보와 지도는 실제 API. 일부 정보는 데이터가 없을 때 생략된다. |
| 장소 저장 | 저장/해제, 비로그인 인증 유도 | 미구현 / 현재 확인 불가 | 장소 상세 | 상세 화면에 저장 버튼 자체가 없다. |
| 장소 후기 조회 | 실제 방문 후기와 요약 | 미구현 / 현재 확인 불가 | 장소 후기 탭 | 모든 장소에 고정 4.7점·126개 목업 후기를 실제 후기처럼 표시한다. 후기 API도 없다. |
| 후기 작성 | 방문 장소에 인증된 후기 등록 | 일부 구현 | `#/write-review` | 정적 화면만 존재한다. 선택 장소 ID가 전달되지 않고 항상 운현궁이며 비로그인도 진입한다. |
| 코스 바구니 | 장소 추가·중복 방지·조회·삭제 | 구현 완료(코드) / 브라우저 차단 | `/api/course-basket/*` | 실제 API와 인증 복귀 코드가 있다. 계정 부재로 mutation 완료 검증은 차단됐다. |
| 코스 조건 입력 | 출발지, 시작 방식, 시간, 공개 범위 | 구현 완료 | `#/course-conditions` | 카카오 출발지 검색, 지금 바로/예약, PRIVATE/PUBLIC 상태 변경 확인. |
| 코스 미리보기 | FAST/EASY/QUIET 후보와 경로·시간 조정 | 일부 구현 | `/api/courses/preview`, 코스 결과 컴포넌트 | FE/BE 구현은 크지만 관련 FE 테스트가 다수 실패하고 인증된 E2E는 차단됐다. PLEASANT는 문서상 후속 범위다. |
| 코스 저장·목록·시작 | 선택 코스 저장, 예정/진행/완료 목록, 시작 | 일부 구현 | `/api/courses`, `/:id/start`, `/:id/complete`, `/:id/replan` | 서버 계약과 화면 연결은 존재한다. 로그인 계정이 없어 브라우저 완료 검증은 차단됐다. |
| 공개 코스 탐색 | 공개된 다른 사용자 코스를 조회·가져오기 | 일부 구현 | 코스 홈 | 공개 범위는 DB/DTO에 추가됐지만 공개 목록 API가 없고 FE는 하드코딩 `publicCourses`를 사용한다. |
| 여행 진행 | 위치 기반 다음 목적지·상태·재계산·완료 | 일부 구현 | active-course 및 travel route group | 저장 코스 화면 일부는 서버 연결. 다수 travel 화면은 정적 프로토타입이며 GPS 방문 판정은 명시적 제외 범위다. |
| 촬영지 카메라 | 장면 ID 기반 카메라·비교·공유 | 일부 구현 | camera/shot-result, media filming API | 실제 카메라 세션 코드가 있으나 등록된 임시 에셋은 ID 1 중심이고 실기기 HTTPS 권한 QA가 필요하다. |
| MY·설정·기록 | 실제 프로필, 설정 저장, 여행/후기 기록 | 일부 구현 | `#/my`, `/api/v1/members/me`, settings/record routes | 프로필 조회만 실제 API다. 알림·개인정보·지원·기록 상당 부분은 정적 화면이다. |

## 2. 현재 실제 사용자 흐름

| 흐름 | 시작 화면 / 사용자 행동 | 관련 화면 | 호출 API | 성공 조건 | 실패 가능 지점 |
|---|---|---|---|---|---|
| 지도에서 외부 장소 찾기 | 지도 → 검색 열기 → 키워드 입력 → 카카오 장소 선택 | map, inline result, bottom sheet | places, place-search/kakao, crowding | 선택 마커와 장소 작업 시트 노출 | 외부 검색 지연, 관련도, 좌표 없음 |
| Explore에서 큐레이션 장소 보기 | Explore → 카드 스크롤 → 상세 선택 → 뒤로가기 | explore, place/event/media detail | trends, media, events, place detail | 실제 콘텐츠와 상세 표시 | API 부분 실패, 뒤로가기 스크롤 초기화 |
| 장소를 코스에 담기 | 장소/지도 시트 → 코스에 담기 | place/map → login 또는 basket | course-basket | 로그인 후 원래 경로 복귀 및 중복 없는 추가 | 인증 만료, 복귀 상태 소실, 중복 클릭 |
| 코스 생성 | 코스 조건 → 출발지 검색 → 시작 방식·공개 범위 → 체류시간 → 미리보기 | course-conditions, course-place-times, compare | place-search/kakao, course-basket, courses/preview | 전체 장소가 포함된 후보와 조정 가능 일정 | 빈 바구니, 인증, 경로 공급자, COURSE4222 |
| 코스 저장·시작 | 미리보기 확정 → 저장 → 예정 목록 또는 즉시 시작 | compare, course-home, saved/active course | courses POST/GET/start | 선택 전략·경로·공개범위가 보존되고 상태 전이 | 인증, 기존 IN_PROGRESS 충돌, UI 테스트 회귀 |
| 촬영지 장면 촬영 | 장소 촬영지 탭 → 장면 → 카메라 → 촬영/비교/공유 | filming, camera, shot-result | filming-location API, MediaDevices | 올바른 장면 ID와 참고 이미지, 카메라 lifecycle | HTTPS/권한/실기기, 임시 권리 에셋 |

## 3. Recent Change Risk

| 변경 기능 | 변경 파일 | 예상 영향 범위 | QA 우선순위 |
|---|---|---|---|
| 코스 공개/비공개 저장 | `CourseCreateRequest`, `Course`, `CourseService`, SQL migration, `ProductFlow.jsx` | DB validation, create contract, 기본값, 화면 상태 전달 | Critical |
| 내 코스/공개 코스 홈 개편 | `CourseHome.jsx`, `courseHomeModel.js`, `styles.css` | 인증 리다이렉트, 3개 상태 목록, 모바일 가로 카드, 가져오기 중복 | Critical |
| 코스·지도 경험 대규모 polish | 최근 `d9638c3`, CoursePreviewResults/VWorldMap/ProductFlow/CSS | 시트 snap, 지도-타임라인 선택 동기화, 경로 전환, 작은 화면 | Critical |
| AI MCP 코스 통합 | 최근 `be8234b`, AI guide/course/controller/client | LLM 오류·인증·장소 참조·코스 저장 | High |
| 지도 HOT·혼잡도·검색 | map filters, mock crowding, place trend | 공개 API, 필터 빈 상태, 위치/지도 overlay | High |
| 장면 카메라 | SceneCameraSession/Screens/UI | 권한, StrictMode, blob URL 정리, 실기기 | High |

## 4. QA 케이스 및 최종 상태

| ID | 기능 | 시나리오 | 사전 조건 | 테스트 절차 | 기대 결과 | 중요도 | 상태 |
|---|---|---|---|---|---|---|---|
| QA-001 | 지도 | 390×844 첫 진입 | 정상 FE/BE | map 진입, DOM/화면/overflow 확인 | 지도·헤더·하단 탭이 잘림 없이 표시 | Critical | PASS |
| QA-002 | 지도 필터 | HOT 필터 선택 | map | HOT 클릭, pressed/class 확인 | 선택 상태가 즉시 반영 | High | PASS |
| QA-003 | 통합 검색 | 경복궁 검색 | 정상 공개 API | 검색창 열기, 입력, 제출, 첫 결과·로딩 종료 확인 | 내부·카카오 통합 목록에서 정확 일치 `경복궁`이 첫 결과 | Critical | PASS |
| QA-004 | 지도 선택 | 카카오 경복궁 선택 | QA-003 | 정확 결과 클릭, 마커·시트·레이아웃 확인 | 선택 마커와 주소·CTA 시트 표시 | Critical | PASS |
| QA-005 | 길찾기 | 현재 위치에서 경로 비교 | 위치 권한·실기 좌표 | 위치 CTA와 길찾기 실행 | 권한별 안내, 도보/대중교통/택시 비교 | Critical | BLOCKED |
| QA-006 | 인증 복귀 | 비로그인 코스 담기 | 미로그인 | 지도 시트 `코스에 담기` 클릭 | 로그인 이동 및 복귀 경로 저장 | High | PASS |
| QA-007 | Explore | 트렌드·작품·행사 초기 조회 | 정상 공개 API | explore 진입, 로딩 종료·콘텐츠 확인 | 세 섹션이 실제 API 데이터로 표시 | Critical | PASS |
| QA-008 | 모바일 스크롤 | Explore 끝 스크롤 시 내비 유지 | 390×844 | 650px 스크롤, nav rect 확인 | 하단 nav가 viewport에 고정 | High | PASS |
| QA-009 | 상세 이동 | 트렌드 카드 → 실제 장소 상세 | QA-007 | 첫 트렌드 카드 클릭 | 올바른 place ID/제목 표시 | Critical | PASS |
| QA-010 | 뒤로가기 | 상세에서 Explore로 복귀 | QA-009, scrollTop 509 | 브라우저 back 후 scrollTop 확인 | 이전 탐색 위치 복원 | Medium | BLOCKED (코드·자동 회귀 PASS, 실브라우저 최종 재검증 환경 차단) |
| QA-011 | 장소 상세 | 실제 장소 기본 정보 | place ID 1 | 직접 진입, API 로딩·주소·지도 확인 | 펜앤커피 실제 정보 표시 | Critical | PASS |
| QA-012 | 장소 탭 | 정보↔후기 전환 | QA-011 | 후기 탭 클릭 | 선택 탭과 패널이 일치 | High | PASS |
| QA-013 | 새로고침 | 장소 상세 hash 유지 | QA-011 | reload 후 URL/DOM 확인 | 같은 장소 상세 재조회 | High | PASS |
| QA-014 | 장소 저장 | 저장/해제와 인증 경계 | 장소 상세 | 저장 버튼 탐색·클릭 시도 | 저장 CTA 존재, 비로그인 로그인 유도 | High | FAIL |
| QA-015 | 후기 데이터 | 장소별 실제 후기 무결성 | place ID 1 | 후기 탭의 수·작성자·내용과 코드/API 비교 | 장소별 서버 후기 또는 명확한 빈 상태 | High | FAIL |
| QA-016 | 후기 작성 | 비로그인에서 선택 장소 후기 진입 | place ID 1, 미로그인 | `후기 남기기` 클릭 | 로그인/방문 자격 확인, 선택 장소 유지 | High | FAIL |
| QA-017 | MY | 비로그인/만료 상태 | 미로그인 | my 진입·reload | 명확한 로그인 CTA와 안전한 빈 프로필 | High | PASS |
| QA-018 | 알림 설정 | 선택 상태 변경 | my → notifications | `필수만` 클릭 후 class 확인 | 기존 선택 해제·새 선택 반영 | Medium | FAIL |
| QA-019 | AI 가이드 | 빈 입력·요청 중 중복 제출 | ai-guide | 빈/입력 후 버튼, 전송 직후 disabled 확인 | 빈 입력 비활성, 요청 중 중복 방지 | High | PASS |
| QA-020 | AI 가이드 | 정상 추천 응답 | 유효 로그인·AI key/quota | 프롬프트 전송 | 실제 추천 또는 구조화 오류 | High | BLOCKED |
| QA-021 | 코스 조건 | 카카오 출발지 검색·선택 | course-conditions | 장소 검색, 안국역 선택 | 출발지 이름·주소·좌표 draft 반영 | Critical | PASS |
| QA-022 | 코스 조건 | 지금 바로/예약 전환 | QA-021 | 시작 방식 전환 | 예약 필드와 즉시 시작 안내가 맞게 전환 | High | PASS |
| QA-023 | 코스 공개범위 | PRIVATE↔PUBLIC | QA-021 | 공개 radio 선택 | 단일 선택 및 draft 반영 | Critical | PASS |
| QA-024 | 코스 홈 | READY/IN_PROGRESS/COMPLETED 실제 목록 | 유효 로그인 | course-home 진입·탭 전환 | 실제 계정 코스와 빈/오류/재시도 | Critical | BLOCKED |
| QA-025 | 장바구니 | 추가·중복·삭제 | 유효 로그인 | 같은 장소 2회 추가, 목록, 삭제 | 중복 1건, 카운트 일치, 삭제 반영 | Critical | BLOCKED |
| QA-026 | 코스 생성 | preview→save→start E2E | 로그인·바구니 2곳 이상 | 3단계 완료, 확정, 시작 | 전체 장소·선택 경로·공개범위 보존 | Critical | BLOCKED |
| QA-027 | 공개 코스 | 다른 사람 코스 가져오기 | 로그인 | 공개 카드 import 연속 클릭 | 장소별 중복 없이 basket 반영 | High | BLOCKED |
| QA-028 | 장면 카메라 | 권한 허용/거부·촬영·재촬영 | HTTPS 실기기, scene ID 1 | 카메라 전 흐름 | lifecycle·비교·공유/다운로드 정상 | Critical | BLOCKED |
| QA-029 | 인증 성공 | 가입·로그인·복귀 | 테스트 계정 정책/자격 증명 | 유효 정보 제출 | 토큰 저장, 원래 화면 복귀 | Critical | BLOCKED |
| QA-030 | 라우팅 | 알 수 없는 hash | `#/not-a-route` | 직접 진입 | 안전하게 지도 화면 fallback | Low | PASS |
| QA-031 | 소형 모바일 | 320×568 지도와 nav | 정상 공개 API | viewport 변경, screenshot/rect/overflow 확인 | CTA·nav·지도 핵심 UI가 viewport 안에 존재 | High | PASS |
| QA-032 | FE 회귀 | 전체 단위 테스트 | 의존성 설치됨 | `npm test` | 전 테스트 통과 | Critical | PASS (302/302) |
| QA-033 | BE 회귀 | 전체 단위/통합 테스트 | DB 접근 가능 | `bash ./gradlew test` | 전 테스트 통과 | Critical | PASS (383 total, 0 fail, 1 skip) |
| QA-034 | FE 빌드 | production build | 의존성 설치됨 | `npm run build` | 빌드 성공 | High | PASS |
| QA-035 | 설정 저장 | 알림 선택 저장·재진입 | notifications | 값을 변경하고 저장 후 재진입 | 변경값이 유지 | Medium | FAIL |
| QA-036 | 후기 등록 | 실제 제출과 네트워크 | 로그인·방문 자격·등록 승인 | 후기 등록 직전까지 확인 | 단일 POST, 성공/실패 피드백, 실제 목록 반영 | High | BLOCKED |
| QA-037 | Explore 전체 검색 | 장소·작품·행사 서버 전체 데이터 검색 | 정상 공개 API | 초기 6개 밖의 `사내맞선`, `온정옥` 검색·빈 결과·지우기·상세 이동 | 장소·작품·진행 중 행사를 각각 keyword API로 조회하고 일치 카드만 표시, 지우면 전체 복원 | High | PASS |
| QA-038 | 행사 상세 | 문의 재배치와 주최 미노출 | `#/event-detail/1` | 기본 정보의 방문 정보 순서·전화 링크·DOM·overflow 확인 | 요금 아래 문의 표시, 전화 링크 유지, 주최 미표시 | Medium | PASS |

## 5. 버그

### BUG-001 — 장소와 무관한 고정 후기 작성 화면에 비로그인 진입

- 중요도: High
- 관련 기능: 장소 상세, 후기 작성, 인증
- 재현: `#/place/1` → `후기 남기기`
- 기대 결과: 로그인/방문 자격을 확인하고 펜앤커피 후기 작성 화면을 연다.
- 실제 결과: 비로그인 상태로 `#/write-review`에 진입하며 장소명이 항상 `운현궁`이다.
- Console/Network: 등록 API 호출 전 단계이며 오류 없음.
- 관련 API: 후기 API 없음.
- 관련 파일: `FE/src/pages/ProductFlow.jsx:2135`, `FE/src/pages/ProductFlow.jsx:4514`
- 추정 원인: route에 place ID를 전달하지 않고 write-review 분기가 고정 문자열·고정 화면으로 작성됐다.

### BUG-002 — 모든 장소에 실제처럼 보이는 고정 후기 126개 노출

- 중요도: High
- 관련 기능: 장소 후기, 데이터 신뢰성
- 재현: 임의 장소 상세 → 후기 탭
- 기대 결과: 해당 장소의 실제 후기 또는 후기 없음/준비 중 상태.
- 실제 결과: 4.7점, 126개, 민서·지우 후기와 사진이 장소와 무관하게 반복된다.
- Console/Network: 후기 조회 요청 자체가 없다.
- 관련 API: 없음.
- 관련 파일: `FE/src/components/placeReviewPreviewModel.js:96`, `FE/src/pages/ProductFlow.jsx:2161`
- 추정 원인: `PLACE_REVIEW_SUMMARY`/fixture를 production 화면에 직접 주입한다.

### BUG-003 — 기획 1순위인 장소 저장 UI/연동 부재

- 중요도: High
- 관련 기능: 장소 상세, MY 저장 장소
- 재현: 실제 장소 상세 확인
- 기대 결과: 저장/해제 CTA와 비로그인 인증 유도.
- 실제 결과: 코스 담기와 후기 남기기만 있고 장소 저장이 없다.
- Console/Network: 요청 없음.
- 관련 API: 저장 장소용 Controller/API도 현재 확인되지 않는다.
- 관련 파일: `FE/src/pages/ProductFlow.jsx:2188`
- 추정 원인: 현재 상세 액션에서 저장 기능이 연결되지 않았다.

### BUG-004 — 공개 범위 저장과 ‘다른 사람 코스’ 데이터 소스 불일치

- 중요도: High
- 관련 기능: 최근 추가된 course visibility, 공개 코스
- 재현: 코드/API 구조 비교, 코스 홈 공개 카드 확인 시도
- 기대 결과: PUBLIC 코스를 서버에서 조회해 실제 사용자 공개 코스로 표시.
- 실제 결과: create에는 visibility가 저장되지만 공개 목록 endpoint가 없고 FE는 하드코딩 배열을 표시한다.
- Console/Network: 공개 목록 네트워크 요청 없음.
- 관련 API: `/api/courses`는 인증된 내 목록만 조회.
- 관련 파일: `FE/src/components/courseHomeModel.js:63`, `FE/src/components/CourseHome.jsx:239`, `BE/src/main/java/com/ddemachim/server/domain/course/controller/CourseController.java`
- 추정 원인: persistence 작업과 공개 discovery API/FE 데이터 전환이 함께 완료되지 않았다.

### BUG-005 — 해결됨: FE 회귀 테스트 실패

- 중요도: High
- 관련 기능: 코스 결과, 카메라 lifecycle, trend/map/search 등
- 재현: `npm test`
- 해결 결과: 2026-08-24 기준 **290 PASS, 0 FAIL**.
- 수정 내용: timeline DOM collection 방어 처리, 지도 선택 focus 동기화, EASY 선택 경로 기준 고도 표시, 촬영지 ID별 참고 이미지 해석 복구, 최신 UI/API 계약에 맞춘 회귀 assertion 갱신.
- 관련 파일: `FE/src/components/CoursePreviewResults.js`, `FE/src/sceneCamera/reference.js` 및 관련 테스트.
- 검증: 전체 FE 테스트와 `npm run build` 성공.

### BUG-006 — 해결됨: BE 회귀 테스트 실패

- 중요도: High
- 관련 기능: 코스 최대 장소수, 혼잡도 설정, 장소 트렌드 상세, TMAP, Kakao 설정
- 재현: `bash ./gradlew test`
- 해결 결과: 2026-08-24 기준 **382 total, 381 PASS, 0 FAIL, 1 SKIP**.
- 수정 내용: 서버의 코스 장소 최대 5개 검증 복구, 새 장소 메뉴 저장소 fixture 추가, 설정 파일 확장자 경로 수정, JWT 테스트 비밀키 격리, TMAP 도보 step 계약 반영.
- 관련 파일: `CoursePreviewRequest.java` 및 해당 BE 테스트 파일.
- 검증: 전체 BE 테스트와 `bash ./gradlew assemble` 성공.

### BUG-007 — 알림·개인정보 설정 컨트롤이 클릭돼도 상태가 바뀌지 않음

- 중요도: Medium
- 관련 기능: MY 설정
- 재현: MY → 알림 설정 → `필수만` 클릭 → 저장
- 기대 결과: 선택 표시 변경 및 재진입 시 유지.
- 실제 결과: `적당히`가 계속 고정 선택이며 저장은 단순 MY 이동이다.
- Console/Network: 요청 없음.
- 관련 API: 없음.
- 관련 파일: `FE/src/pages/ProductFlow.jsx:4634`, `FE/src/pages/ProductFlow.jsx:4636`
- 추정 원인: 버튼이 정적 markup이고 state/onClick/persistence가 없다.

### BUG-008 — 코드 해결됨 / 브라우저 재검증 대기: Explore 상세 복귀 시 스크롤 위치 소실

- 중요도: Medium
- 관련 기능: 탐색, 뒤로가기
- 재현: Explore `scrollTop=509` → 카드 상세 → 브라우저 back
- 기대 결과: 이전 탐색 위치 복원.
- 기존 실제 결과: `scrollTop=0`. 1차 수정 후 비동기 목록이 아직 짧을 때 509px 요청이 281px로 clamp되는 것도 실브라우저에서 확인했다.
- 수정 내용: Explore root의 실제 scroll 이벤트 값을 화면 세션에 저장하고, 상세 복귀 시 원래 목표값을 별도로 유지한다. 트렌드·촬영작·행사 응답으로 콘텐츠 높이가 증가할 때 목표 위치에 도달할 때까지 복원을 재적용하며, 중간 clamp 이벤트는 저장값을 덮어쓰지 않는다.
- 자동 검증: clamp `281 → 509` 비동기 높이 증가 회귀 테스트 포함, 2026-08-24 `npm test` **293 PASS, 0 FAIL**, `npm run build` 성공.
- 브라우저 재검증: 수정 전 `509 → 0`, 1차 수정 `509 → 281`까지 실제 390×844 브라우저에서 재현했다. 최종 수정 후에는 현재 실행 환경에서 PostgreSQL 네트워크 연결과 새 브라우저 localhost 접근 정책이 차단되어 `BLOCKED`; 정상 BE 환경에서 동일 절차 1회 재확인이 필요하다.
- 관련 파일: `FE/src/pages/ProductFlow.jsx`, `FE/src/utils/scrollRestoration.js`, `FE/src/utils/scrollRestoration.test.js`
- 확인된 원인: 비동기 목록 로드 전에 복원 가능한 최대 스크롤이 작고, 그 clamp된 scroll 이벤트가 원래 저장 위치를 덮어썼다.

### BUG-009 — 해결됨: 검색 정확 일치 결과가 부분 일치 뒤에 노출

- 중요도: Medium
- 관련 기능: 통합 장소 검색
- 재현: 지도 검색에서 `경복궁` 입력
- 기대 결과: 정확 일치 경복궁이 상단 또는 통합 관련도 순으로 표시.
- 기존 실제 결과: 내부 DB의 ‘경복궁점’ 음식점 20건 뒤 카카오 섹션에서 정확 장소가 나왔다.
- 수정 내용: 내부·카카오 결과를 하나의 목록으로 합치고 이름 정확 일치, 접두 일치, 부분 일치, 기타 메타 일치 순으로 안정 정렬한다. 공급자 구분은 각 결과 라벨로 유지한다.
- 브라우저 검증: 390×844 지도 검색에서 `경복궁` 첫 결과가 `경복궁`, 목록 라벨이 `관련도 높은 장소`임을 확인했다. 가로 overflow와 console warning/error는 없었다.
- 자동 검증: 내부 부분 일치 20건보다 카카오 정확 일치가 먼저 오는 회귀 테스트 포함, 전체 FE **296 PASS, 0 FAIL**, production build 성공.
- 관련 API: `/api/places?keyword=경복궁`, `/api/place-search/kakao?query=경복궁`
- 관련 파일: `FE/src/utils/placeSearch.js`, `FE/src/utils/placeSearch.test.js`, `FE/src/pages/ProductFlow.jsx`
- 확인된 원인: 공급자별 섹션을 고정 순서로 연결하고 통합 relevance scoring을 하지 않았다.

### BUG-010 — 알 수 없는 hash를 지도에 표시하지만 URL을 정규화하지 않음

- 중요도: Low
- 관련 기능: 라우팅
- 재현: `#/not-a-route` 직접 진입
- 기대 결과: `#/map`으로 replace하거나 명시적 404.
- 실제 결과: 지도는 보이지만 URL은 잘못된 hash 그대로다.
- Console/Network: 오류 없음.
- 관련 파일: `FE/src/pages/ProductFlow.jsx:195`
- 추정 원인: `readHash`가 화면 값만 map으로 대체하고 history URL은 변경하지 않는다.

## 6. QA Summary

- Total: 38
- PASS: 23
- FAIL: 5
- BLOCKED: 10

차단 항목은 코드 PASS로 간주하지 않았다. 주 원인은 테스트 계정/자격 증명 부재, 실제 위치·카메라 권한 및 HTTPS 실기기 조건, 후기 등록의 외부 행위 확인 부재다.

## 7. Severity Summary

- Critical: 0
- High: 4
- Medium: 1
- Low: 1

## 8. Critical / High Bugs

1. BUG-001: 잘못된 장소로 비로그인 후기 작성 진입
2. BUG-002: 고정 목업 후기를 실제 사용자 후기처럼 표시
3. BUG-003: 장소 저장 기능 부재
4. BUG-004: 공개 코스 영속화와 공개 목록 UI 데이터 소스 불일치

BUG-005와 BUG-006은 2026-08-24 전체 회귀 재검증으로 해결됐다.

## 9. Spec Mismatch

- 장소 저장은 1순위지만 UI/API가 없다.
- 후기 조회·작성·기록은 실제 데이터가 아니라 장소와 무관한 고정 프로토타입이다.
- 공개 범위는 저장되지만 ‘다른 사람의 코스’는 서버의 PUBLIC 코스가 아니다.
- 알림·개인정보·지원 설정은 저장 가능한 기능처럼 보이지만 동작하지 않는다.
- 여행 진행 다수 화면은 실제 GPS 진행 상태가 아닌 고정 시나리오다.
- 관심 카테고리와 추천 반영은 화면 이후 서버 저장 흐름이 확인되지 않는다.

## 10. UX Issues

- Explore 상세 복귀 스크롤 복원 로직은 수정됐으며, 정상 BE가 연결된 실브라우저에서 최종 회귀 확인이 남아 있다.
- Explore 검색은 장소·촬영 작품·진행 중 행사 API를 250ms debounce 후 병렬 조회하며, 최초 화면에 로드되지 않은 항목도 검색된다.
- 320px 폭에서 AI 가이드 하단 탭 문구가 2줄로 줄바꿈되지만 터치 영역(58×60)은 유지된다.
- 장소 상세 API 오류 화면에는 즉시 재시도 버튼이 없다.

## 11. Regression Risk

- 코스 결과 시트/지도 동기화는 자동 회귀가 복구됐지만 최근 변경 규모가 커 브라우저 E2E 우선순위를 계속 높게 유지한다.
- camera lifecycle/StrictMode 자동 테스트는 복구됐으며, 실제 권한·메모리 해제는 HTTPS 실기기 검증이 남아 있다.
- visibility DB/DTO 변경은 migration 적용 순서와 구버전 데이터 기본값을 함께 검증해야 한다.
- FE 기본 개발 proxy는 8080 고정이므로 다른 로컬 서비스가 점유할 때 완전히 다른 API의 응답을 받을 수 있다. 실행 가이드에 `VITE_API_PROXY_TARGET` 확인 절차가 필요하다.

## 12. Automatically Fixed

- FE: 코스 시트 DOM 안전 처리, 선택 장소 focus 동기화, 선택한 EASY 도보 경로의 고도 표시 조건 수정.
- FE: 촬영지 ID를 무시하고 항상 1번 참고 이미지를 반환하던 카메라 reference 매핑 수정.
- FE: Explore 상세 복귀 시 비동기 목록 높이 변화에도 원래 스크롤 위치를 유지하도록 지연 복원 로직과 회귀 테스트 추가.
- FE/BE: Explore 검색을 로컬 카드 필터에서 장소·촬영 작품·진행 중 행사 서버 keyword 병렬 검색으로 전환하고, 작품 제목 keyword API 계약과 구버전 서버 응답 호환 필터를 추가.
- FE: 행사 상세의 문의를 요금 아래 방문 정보 행으로 이동하고 전화 링크를 유지했으며 주최 표시를 제거.
- FE: 내부·카카오 장소 검색 결과를 통합 관련도 순으로 정렬해 정확 일치를 최상단에 표시.
- BE: 코스 미리보기 요청의 최대 장소 수 5개 검증 복구.
- FE/BE: 최근 UI·DTO·repository·설정 파일 변경과 어긋난 회귀 테스트 계약을 현재 구현에 맞게 갱신.
- 기존 사용자 변경 파일은 보존했다.

## 13. Remaining Issues

1. High: 후기의 장소 ID·인증·실제 API 계약을 연결하고 목업 표시를 제거한다.
2. High: 장소 저장 API/UI를 구현한다.
3. High: PUBLIC 코스 목록 endpoint와 FE 연동을 추가한다.
4. Medium: 설정 화면을 실제 state/API와 연결하거나 ‘준비 중’으로 명확히 표시한다.
5. Blocked: 정상 BE 환경에서 Explore `509px → 상세 → back → 509px` 실브라우저 회귀를 1회 확인한다.
6. Blocked: QA 계정과 HTTPS 실기기에서 코스 E2E·위치·카메라 회귀를 완료한다.

## 14. Demo Readiness

**NEEDS FIX BEFORE DEMO**

지도 검색, Explore, 장소 기본 상세은 정상 API 환경에서 실제 사용자 흐름으로 동작하고 FE 302개·BE 383개(1 skip)의 자동 회귀도 0 fail이다. 그러나 후기 데이터가 실제처럼 보이는 고정 목업이고 선택 장소까지 틀리며, 최근 핵심인 코스 공개/목록/저장/시작은 인증된 브라우저 E2E가 끝나지 않아 전체 서비스 데모 기준으로는 추가 수정과 재검증이 필요하다.
