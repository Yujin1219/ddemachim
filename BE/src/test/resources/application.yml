spring:
  datasource:
    url: jdbc:h2:mem:ddemachim;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none

jwt:
  secret: test-only-jwt-signing-key-0123456789
  access-token-expire-time: 3600000
